<div>
  <div>
    <%= render partial: "layouts/header" %>
    <%= render partial: "layouts/sidebar" %>
  </div>
  <div class="pt-[72px] pl-[184px]">
    <% if (WorksSchedule.exists?(event_id: @event.id)) && (MembersSchedule.exists?(event_id: @event.id)) %>
    <!--
      <div class="mt-20">
        <div class="w-full overflow-x-scroll border-x-2">
          <table class="border-2">
            <tr>
              <th width="140px" height="32px" class="flex border-r-2 border-b-2 min-w-[140px]"></th>
              <% @first_schedules.each do |schedule| %>
                <th height="32px" class="border-r-2 border-b-2 leading-none min-w-[72px] w-[72px]"><%= schedule.time_zone %></th>
              <% end %>
            </tr>
            <% @members.each do |member| %>
              <tr height="32px" width="140px" align="center" class="text-base font-bold border-b-2 min-w-[140px]">
                <td class="border-r-2 flex justify-center gap-1 py-1">
                  <%= member.name %>
                  <%= Shift.where(member_id: member.id, how_many_days: 1).count %>
                </td>
                <% @first_schedules.each do |schedule| %>
                  <% if MembersSchedule.exists?(schedule_id: schedule.id, member_id: member.id) %>
                    <td class="border-r-2 bg-red-500">
                      <div class="text-white font-bold">休み</div>
                    </td>
                  <% else %>
                    <td class="border-r-2 pb-1">

                    </td>
                  <% end %>
                <% end %>
              </tr>
            <% end %>
          </table>
        </div>
      </div>
      <div class="mt-20 mb-20">
        <div class="w-full overflow-x-scroll border-x-2">
          <table class="border-2">
            <tr>
              <th width="120px" height="32px" class="border-r-2 border-b-2 min-w-[120px]"></th>
              <% @first_schedules.each do |schedule| %>
                <th width="120px" height="32px" class="border-r-2 border-b-2 leading-none min-w-[72px] w-[72px]"><%= schedule.time_zone %></th>
              <% end %>
            </tr>
            <% @works.each do |work| %>
              <tr height="32px" width="120px" align="center" class="text-base font-bold border-b-2 min-w-[120px]">
                <td class="border-r-2">
                  <%= work.title %>
                </td>
                <% @first_schedules.each do |schedule| %>
                  <% if WorksSchedule.exists?(work_id: work.id, schedule_id: schedule.id) %>
                    <td class="border-r-2 min-w-[120px]">
                      <% if @shifts.present? %>
                        <%= Member.find(@shifts.find_by(schedule_id: schedule.id, work_id: work.id).member_id).name %>
                      <% end %>
                    </td>
                  <% else %>
                    <td class="border-r-2 pb-1 bg-gray-500 min-w-[120px]"></td>
                  <% end %>
                <% end %>
              </tr>
            <% end %>
          </table>
        </div>
      </div>
    -->
    <% elsif (WorksSchedule.exists?(event_id: @event.id)) %>
      <div class="flex flex-col justify-center items-center w-full pt-40">
        <%= embedded_svg('fv-shift-image', class: "") %>
        <p class="mt-3 text-center text-2xl text-main-black leading-10"><span class="text-main-blue">スタッフ管理</span><br>から情報を入力してください</p>
      </div>
    <% elsif (MembersSchedule.exists?(event_id: @event.id)) %>
      <div class="flex flex-col justify-center items-center w-full pt-40">
        <%= embedded_svg('fv-shift-image', class: "") %>
        <p class="mt-3 text-center text-2xl text-main-black leading-10"><span class="text-main-blue">仕事管理</span><br>から情報を入力してください</p>
      </div>
    <% else %>
      <div class="flex flex-col justify-center items-center w-full pt-40">
        <%= embedded_svg('fv-shift-image', class: "") %>
        <p class="mt-3 text-center text-2xl text-main-black leading-10"><span class="text-main-blue">スタッフ管理、仕事管理</span><br>から情報を入力してください</p>
      </div>
    <% end %>
  </div>
</div>
